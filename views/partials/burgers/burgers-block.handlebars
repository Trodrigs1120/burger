<li>
	{{burger_name}}
	{{#if devoured}}
	<button class="change-status" data-id="{{id}}" data-newdevour="{{devoured}}">
		I WILL CONSUME THIS BURGER
	</button>
	{{/if}}
	<button class="delete-burger" data-id="{{this.id}}">Remove burger (you monster)</button>
</li>
